# Build trees (any cmake -B directory)
starters/cpp/build*/
# Local bench binaries
starters/cpp/bench/throttle
# LF working copy that run-harness-windows.sh regenerates on every run
starters/cpp/bench/ryzen/.lf/
# Serena cache
.serena/
# macOS finder metadata
.DS_Store
